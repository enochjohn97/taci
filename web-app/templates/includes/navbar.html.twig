<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_dashboard') }}">
            <i class="bi bi-droplet-fill me-2"></i>
            <span>{{ app_name }}</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#topNavbar" aria-controls="topNavbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="topNavbar">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                <li class="nav-item me-3">
                    <a class="nav-link" href="{{ path('app_notifications') }}">
                        <i class="bi bi-bell-fill"></i>
                        {% if unread_notifications_count is defined and unread_notifications_count > 0 %}
                            <span class="badge bg-danger rounded-pill">{{ unread_notifications_count }}</span>
                        {% endif %}
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-circle me-1"></i>
                        {{ app.user.username }}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                        {% if is_granted('ROLE_SUPER_ADMIN') %}
                            <li><a class="dropdown-item" href="{{ path('app_delegates_index') }}"><i class="bi bi-people me-2"></i> Delegates</a></li>
                            <li><hr class="dropdown-divider"></li>
                        {% endif %}
                        <li><h6 class="dropdown-header">Settings</h6></li>
                        <li><a class="dropdown-item" href="{{ path('app_settings_profile') }}"><i class="bi bi-person me-2"></i> Profile</a></li>
                        {% if is_granted('ROLE_SUPER_ADMIN') %}
                            <li><a class="dropdown-item" href="{{ path('app_settings_admin') }}"><i class="bi bi-shield-lock me-2"></i> Admin Panel</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_settings_admin_users') }}"><i class="bi bi-people-fill me-2"></i> Sub-Admins</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_settings_audit_log') }}"><i class="bi bi-journal-text me-2"></i> Audit Log</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_settings_system_info') }}"><i class="bi bi-info-circle me-2"></i> System Info</a></li>
                        {% endif %}
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i> Logout</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
