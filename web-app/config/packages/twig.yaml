twig:
    file_name_pattern: '*.twig'
    globals:
        app_name: '%env(APP_NAME)%'
        admin_email: '%env(ADMIN_EMAIL)%'
        mercure_public_url: '%env(default::MERCURE_PUBLIC_URL)%'

when@test:
    twig:
        strict_variables: true
